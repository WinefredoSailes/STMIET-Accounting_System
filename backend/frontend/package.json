{
  "name": "stmiet-ui",
  "version": "1.0.0",
  "private": true,
  "description": "STMIET Accounting System — server-rendered UI (Django templates + HTMX + Tailwind). No JS framework.",
  "scripts": {
    "dev": "tailwindcss -i ./src/input.css -o ../static/css/output.css --watch",
    "build": "tailwindcss -i ./src/input.css -o ../static/css/output.css --minify",
    "test": "node --test \"tests/**/*.test.mjs\""
  },
  "devDependencies": {
    "chart.js": "^4.4.4",
    "tailwindcss": "^3.4.0"
  }
}
